<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projet.miniflux"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="18" />
        
    <uses-permission android:name="android.permission.INTERNET" />    
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
    <activity
            android:name="enseirb.t2.miniflux.MainActivity"
             android:label="@string/app_name"   >
            <!-- android:configChanges="keyboardHidden|orientation|screenSize" 
            android:screenOrientation="landscape" // pour mettre sous forme de tablette-->
           
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    <activity
            android:name="enseirb.t2.miniflux.HomeActivity"
            android:label="@string/home_label" >   
     </activity>    
           
    <!-- New activity  -->
        <activity
        	android:name="enseirb.t2.miniflux.SubscriptionActivity"
        	android:label="@string/subscription"
        	android:parentActivityName="enseirb.t2.miniflux.MainActivity" >
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="enseirb.t2.miniflux.MainActivity" />
        </activity>
        
    </application>

</manifest>
